const sum = (a,b) => {                    // all function can be written like implicat function but only " only if single value is return "
    console.log(a+b);              
};

const cube = (n) => {
     console.log(n*n*n);
}; 

// implict return 
const mul =(a,b) => a*b ;
const sub =(a,b) => a*b;